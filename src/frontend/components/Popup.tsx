import "@bentley/icons-generic-webfont/dist/bentley-icons-generic-webfont.css";
import "./App.css";


// export const popup = () => {
//   return (
//     <div className="popup">
//       <Button title="Name" id="newIModel" onClick={() => newWindow()}>Select new IModel</Button>
//     </div>
//   );
// };

//const BrowserWindow = electron.remote.BrowserWindow;
// const notifyBtn = document.getElementById("newIModel");

// export const onClick = () => {
//   //newWindow();
// };

/* const newWindow = () => {
  if (notifyBtn)
    notifyBtn.addEventListener("click", function () {
      const win = new BrowserWindow({
        width: 800,
        height: 600,
        center: true,
        frame: true,
        transparent: false,
        movable: true,
          webPreferences: {
          nodeIntegration: true,
          },
      });

      win.loadURL("https://qa-connect-webportal.bentley.com/SelectProject/Index");
      win.on("close", function () { win.close() })
      win.show();
    });
  console.log("Opening Window");
}; */
